# Session Handoff — 2026-04-27T06:31:42

## Changed Files

Unstaged at handoff time (not committed — user-made / mid-debug):
- `.claude-plugin/plugin.json` (+1 -1) — `${CLAUDE_PLUGIN_ROOT}/dist/index.js` → `${CLAUDE_PLUGIN_ROOT}dist/index.js` (slash removed). Done during the MCP-loading debug at end of session; outcome unconfirmed (see Context).
- `.bones/tasks/lspm-8cu.md` (+7 -3) — likely `bn close` frontmatter update (status: closed) plus minor spacing. Not material; safe to stage with next commit.

Recent commits (this session's work in **bold**):
- **00e2ac6** lspm-xxk: scope Phase 1 acceptance task
- **9b5fe27** lspm-8cu: R9 using-lsp-mcp skill — polyglot symbol_search-first agent guidance
- 13a0b31 lspm-4vb: close — R7b dynamic tool schemas, all 26 SCs satisfied
- f216be0 lspm-4vb: R7b dynamic tool schemas — lang/manifest/via enums from active manifests
- 06a819a lspm-4vb: scope R7b — dynamic tool schemas
- 8aca216 lspm-zw9: flip final SC (commit 1054ba0); close task
- 1054ba0 lspm-zw9: close — R7 set_primary MCP tool, all SCs satisfied
- ab057ee lspm-zw9: scope R7 — set_primary MCP tool
- c8e6028 lspm-rot: close — R6 list_languages MCP tool, all SCs satisfied
- 9672791 lspm-rot: R6 list_languages MCP tool — closes lspm-cnq list_languages SC

## Tasks Completed

- **lspm-8cu** — R9 using-lsp-mcp skill (polyglot symbol_search-first agent guidance). Closed via bn close with all-criteria gate passing.

## Tasks Open

- **lspm-y5n** (epic) — lsp-mcp marketplace: polyglot workspace/symbol that agents reach for over grep
- **lspm-cnq** (sub-epic) — Phase 1: Core plugin + multi-candidate routing — 11/11 children including lspm-xxk; awaiting acceptance task closure
- **lspm-erd** (sub-epic) — Phase 2: Fork wrappers + settings + authoring — blocked by lspm-cnq
- **lspm-xxk** (task) — Phase 1 Acceptance: README + zero-env-vars smoke + cold CC session polyglot demo

## Tasks Ready

- **lspm-xxk** — Phase 1 Acceptance task, only unblocked task. Two deliverables: (1) README stale-only updates [agent can do]; (2) cold CC session polyglot demo [user runs in their environment].

## Decisions Made

- **Skill-reviewer subagent as the content-task acceptance gate.** No code tests exist for skill content; using `plugin-dev:skill-reviewer` agent in a 3-iteration loop with explicit acceptance criteria + rejected-finding documentation. Captured as a reference memory (`skill_reviewer_as_content_gate.md`). Pattern applies to Phase 2 skills (`authoring-lsp-plugin`, `lsp-mcp-settings`).
- **"Example from a dev box" disclaimer on inline enum values** — adversarial-planning's Stale Enum Values mitigation became a new SC mid-flight. Quoted enum values in skill content carry the disclaimer; quoted enum *shapes* don't need it. Reviewer rejected finding M3 (don't extend disclaimer to stable manifest names) accepted.
- **Three cross-language examples kept inline** (no progressive disclosure split). Body landed at 1822 words, well under the 2200 split threshold; flat structure preserves the activation flow.
- **Tool-surface section split correctly** per `src/mcp-server.ts`: `hover {file, pos, via?}` (pos required) vs `outline`/`diagnostics {file, via?}` (no pos); `call_hierarchy_prepare {file, pos, via?}` vs `incoming_calls`/`outgoing_calls {item, via?}`. Reviewer iteration 1 caught this — would have shipped invalid MCP-call examples otherwise.
- **Iteration-2 rejections documented**: "three dials" framing kept (langs is a discovery-scope filter, not a routing dial); "example from a dev box" not extended to manifest names (manifest names are stable file-shipped contract, not runtime enum drift). Both rejections preserved in the iteration-3 prompt context to suppress phantom re-flagging.
- **Acceptance task scoped as a single bundle** (lspm-xxk) covering README + zero-env-vars smoke + cold CC demo. Not split into separate tasks; the four remaining sub-epic SCs naturally cohere as one acceptance gate.

## Context for Next Session

**Where we left off:**
The session closed lspm-8cu (R9 skill ships) and scoped lspm-xxk (acceptance task). User chose "Stop — let me review first" before agent proceeds with README updates. Agent asked which polyglot repo to reference for the cold-session demo; user **interrupted that question** without answering, then attempted to debug why the lsp-mcp MCP server wasn't loading in CC.

**Open MCP-loading question (URGENT — flag for next session):**

The user reinstalled the plugin (`/plugin uninstall lsp-mcp` → `/plugin install lsp-mcp` → `/reload-plugins`), then re-added the marketplace (`/plugin marketplace add ./` → `/plugin install lsp-mcp` → `/reload-plugins`). During this debug, they edited `.claude-plugin/plugin.json`:

```diff
-      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/index.js"]
+      "args": ["${CLAUDE_PLUGIN_ROOT}dist/index.js"]
```

This change is **not committed** and the user did not confirm whether the MCP is now loading. Possibilities:

1. **`${CLAUDE_PLUGIN_ROOT}` already includes a trailing slash** in CC's current plugin-cache resolver — in which case the original form yielded `<cache>//dist/index.js` (double slash; some shells/Node tolerate, some don't), and the new form is the correct fix.
2. **`${CLAUDE_PLUGIN_ROOT}` does NOT include a trailing slash** — in which case the new form yields `<cache>dist/index.js` (no separator) and is broken; the original form was correct.
3. **CC plugin install changed the cache layout** since lspm-501 originally verified the path resolution.

Lspm-501's empirical-verification work (commit 633ea50) confirmed `${CLAUDE_PLUGIN_ROOT}/dist/index.js` resolved correctly at install time. If that has regressed, it's a CC-platform change worth investigating.

**Next session should:**
1. Ask user: did MCP load with the slash-removed form? If yes → commit the change, also flag this as a project memory ("CLAUDE_PLUGIN_ROOT trailing-slash convention drift"). If no → revert the edit and dig deeper (check stderr from `/mcp` debug mode for actual error).
2. If MCP is loading: agent can proceed with lspm-xxk Deliverable 1 (README updates) once user confirms. User still needs to name the polyglot repo for the demo (was about to answer when MCP debug intervened).
3. If MCP still not loading: that becomes the priority — acceptance task sign-off requires `/mcp` showing `lsp` connected (Sign-Off item #4).

**Other context:**
- 243 tests still green; bun build clean. No code regressions from R9.
- One new reference memory: `skill_reviewer_as_content_gate.md` (added to MEMORY.md index).
- Phase 2 (lspm-erd) is contract-bound to Phase 1's tool surface — README accuracy in lspm-xxk Deliverable 1 propagates into Phase 2 expectations. Ground README in `src/mcp-server.ts`, not memory.
- Prior agent left a note in lspm-cnq's log (2026-04-20T02:10:30Z) that parent epic lspm-y5n still has stale paths flagged but not edited — verify next session whether those need attention now that Phase 1 is at acceptance.
